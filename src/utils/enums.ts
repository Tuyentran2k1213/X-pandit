export enum Role {
    user = 'User',
    admin = 'Admin'
}

export enum Status {
    active = 'Active',
    pending = 'Pending',
    rejected = "Rejected",
}

export enum EmployeeStatus {
    approved = 'Approved',
    pending = 'Pending',
    onBoarding = 'OnBoarding',
    rejected = 'Rejected'
}

export enum TransactionStatus {
    active = "Active",
    pending = "Pending",
    rejected = "Rejected",
}